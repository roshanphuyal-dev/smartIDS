Model B not implemented: completed-flow classifier phase is not built yet (optional later, but listed in plan).
Alert dedup/update logic: you have thresholded alerts, but not robust “avoid duplicate alerts for same session / update final classification” flow yet.
Strict “single source import everywhere” validation: some scripts still need consistency checks to ensure all paths import exactly one schema/mapping source.